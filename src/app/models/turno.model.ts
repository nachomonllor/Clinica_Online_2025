export interface Turno {
    id: number;
    fecha: string; // Puedes usar Date si lo prefieres
    hora: string;
    especialidad: string;
    especialista: string;
    // Otros campos que necesites
  }

  