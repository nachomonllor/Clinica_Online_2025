import { FiltroTurnosPipe } from './filtro-turnos.pipe';

describe('FiltroTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
